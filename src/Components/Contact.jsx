import styled from "styled-components";

const Contact = () => {
  return <Title1>Contact.</Title1>;
};

const Title1 = styled.h1`
  font-family: var(--first-font);
  font-size: var(--first-size);
`;

export default Contact;
